## ADDED Requirements

### Requirement: Detail panel metadata uses vertical layout
The metadata table in the detail panel SHALL render each field label directly above its value, not on the same row as its value.

#### Scenario: Label appears above value
- **WHEN** the detail panel is open for any item
- **THEN** each metadata label (e.g. "Pages", "Format") is rendered on its own line above the corresponding value
