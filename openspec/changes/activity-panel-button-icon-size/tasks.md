## 1. Implementation

- [x] 1.1 In `sidebar_view.rs`, change `.text_xs()` to `.text_2xl()` on the `div()` in `render_activity_button`

## 2. Verification

- [x] 2.1 Run `cargo check --all-targets` — no compile errors
- [x] 2.2 Launch the app and confirm the activity button symbol is visibly larger and more legible in all three states: idle (`○`), active (`↻ (N)`), and complete (`● (N)`)
